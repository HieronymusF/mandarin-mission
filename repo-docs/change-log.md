# Repo guide 变更记录

## 2026-07-27

- 16:44 +08:00：按用户授权将通知/诊断本地选择切片提交为 `e33aa12` 并推送 `agent/privacy-controls`，创建面向 `main` 的 Draft PR [#27](https://github.com/HieronymusF/mandarin-mission/pull/27)。本地与远端分支 SHA 已核对一致；Project CI run [`30251039467`](https://github.com/HieronymusF/mandarin-mission/actions/runs/30251039467) 已触发，核对时三项 job 尚未全部完成。repo-docs Sync Gate 为 `foreground patch`。Synced through e33aa12 + current documentation patch。
- 16:32 +08:00：在 `agent/privacy-controls` 把通知与分析/崩溃从只读状态扩展为默认关闭、可本机持久化和撤回的选择；页面始终说明服务未接入，不请求通知权限、不发送诊断数据。读取/保存失败、跨 App 重建恢复、200% 字号和 44×44dp 触控语义均有 Widget 回归；Flutter format 84 files / 0 changed、analyze 0 issues、89 tests 与 Debug APK 通过。APK 为 249,096,035 bytes，SHA-256 `e12cec904974f614444df0ede6a5b7012d6075ccbd27657566a0f8b23d5b7759`。Android 模拟器 `emulator-5554` 已验证开/关、不同 PID 恢复、两个唯一可点击的 44dp 开关语义节点与无错误日志，截图为 `app-privacy-controls-final.png`；本轮没有连接 Sony 真机。repo-docs Sync Gate 为 `foreground patch`，更新 README、代码地图与源码证据。Synced through e21e924 + current local privacy-controls patch。
- 16:07 +08:00：按用户授权将 PR #26 从 Draft 转为 ready；实时核对 head `a3510e9`、三项 CI success 与 `CLEAN` 后，以 merge commit `e21e924` 合并到 `main`。本地 `main` 已快进到同一 SHA；合并后的独立 push CI run [`30248150647`](https://github.com/HieronymusF/mandarin-mission/actions/runs/30248150647) 再次通过 `learning-core`、`api`、`mobile`，其中 `mobile` 完成格式、分析、86 tests 与 Debug APK。repo-docs Sync Gate 为 `foreground patch`。Synced through e21e924 + current local documentation patch。
- 15:49 +08:00：Draft PR #26 的最新代码/文档 head `713dced` 已通过 Project CI run [`30247032620`](https://github.com/HieronymusF/mandarin-mission/actions/runs/30247032620)：`learning-core` 22s、`api` 50s、`mobile` 7m41s，三项结论均为 success；`mobile` 内格式、分析、86 tests 与 Debug APK 均通过。PR 保持 Draft，未转 ready 或合并。repo-docs Sync Gate 为 `foreground patch`。Synced through 713dced + current documentation patch。
- 15:40 +08:00：词块乱序功能提交 `fa989e7` 已推送到 `agent/shuffle-build-phrase`，并创建面向 `main@4d3bcc2` 的 Draft PR [#26](https://github.com/HieronymusF/mandarin-mission/pull/26)。本地与远端分支 SHA 已核对一致；最新 head 的 Project CI run [`30246957542`](https://github.com/HieronymusF/mandarin-mission/actions/runs/30246957542) 已触发，核对时三项 job 尚未全部完成。repo-docs Sync Gate 为 `foreground patch`。Synced through fa989e7 + current documentation patch。
- 15:31 +08:00：修复全部 `order_tokens` / `Build the phrase` 共用词块区固定完全倒序的问题。失败测试先证明四词题始终显示 `[3, 2, 1, 0]`；共享 Widget 现于进入步骤时只洗牌一次，答题期间保持稳定，并在三个以上词块时避开正确顺序与旧完全倒序。验证通过：步骤组件 8 tests、Flutter format 82 files / 0 changed、analyze 0 issues、全量 86 tests、Debug APK。APK 为 249,088,629 bytes，SHA-256 `8a37365769d0f1a3fe09b0c9b4a708344bdae66343f2fc2341bdffc383592527`。Android 模拟器 `emulator-5554` 的 Café 01 Step 8 实际显示“咖啡 / 要 / 我 / 一杯”，不是答案“我 / 要 / 一杯 / 咖啡”或旧完全倒序；PID `6616` 无 overflow、FlutterError、fatal 或 `E/flutter`，截图为 `build-the-phrase-shuffled.png`。未操作 Sony 真机。repo-docs Sync Gate 为 `foreground patch`。Synced through fa989e7。
- 14:52 +08:00：App 偏好与服务状态功能提交 `f03220e` 已推送到 `agent/app-preferences-status`，并创建面向 `main@01c9580` 的 Draft PR [#25](https://github.com/HieronymusF/mandarin-mission/pull/25)；代码 head 的 CI run [`30243309445`](https://github.com/HieronymusF/mandarin-mission/actions/runs/30243309445) 与首个文档跟进 head 的 run [`30243857312`](https://github.com/HieronymusF/mandarin-mission/actions/runs/30243857312) 均已通过 `mobile`、`learning-core`、`api`。PR 尚未转为 ready 或合并，真实通知、可选收集控制和 Sony 真机视觉仍是后续边界；合并前必须实时核对最新 head。repo-docs Sync Gate 为 `foreground patch`。Synced through f03220e + current documentation patch。
- 14:30 +08:00：实现 App 偏好与服务状态切片：Settings 新增 App preferences 入口，离线展示界面/内容语言、课程音频、可选麦克风，以及通知、分析/崩溃、账号/同步尚未接入的真实状态；没有新增依赖、权限请求、数据写入或无效开关。用户圈出的共享小标签现使用既有品牌主绿色，黑色状态值、灰色说明、字号和布局不变；颜色测试先因旧样式未显式设色而失败，修正后通过。入口测试先因入口不存在而失败；第一次最终全量复跑又发现既有 onboarding 测试点击被新增首行挤到导航栏后的入口而超时，测试改为将目标滚动到视口中央。最终 Flutter format 82 files / 0 changed、analyze 0 issues、85 tests、Debug APK 通过。APK 为 249,087,183 bytes，SHA-256 `1d51238ad7950dafd7e0995b9c32e83b306f82479c995bf3920661a9471034b0`。Android 模拟器 `emulator-5554` 覆盖安装最终 APK，完成 Settings → App preferences 视觉与 UI hierarchy 检查；前台 PID 的 overflow、FlutterError、fatal 与 `E/flutter` 计数均为 0，截图为 `app-preferences-top.png`、`app-preferences-services.png`。200% 字号由 Widget 测试覆盖，本轮未安装、启动或清除 Sony 真机数据。repo-docs Sync Gate 为 `foreground patch`。Synced through 01c9580 + current `agent/app-preferences-status` working tree。
- 13:33 +08:00：实现公开 MVP 的“首次使用引导”首切片：生产启动在主导航前显示单页价值/离线/账号/本地数据说明，使用版本化异步偏好标记保存完成状态，写入失败留页重试、读取失败放行本地核心课程；Settings 可重看且不改写标记。验证：Flutter analyze 0 issues、83 tests、Debug APK；APK 为 249,083,116 bytes，SHA-256 `f1cf172218683a7fcb76a71d3f9430db4c3aa916d5a2ee329823ec8a292157be`。Android 模拟器完成首次拦截、进入 Journey、冷启动不重复和 Settings 重看闭环，前台日志无 overflow、FlutterError 或 fatal；本轮未在 Sony 安装或清除数据。repo-docs Sync Gate 为 `foreground patch`。Synced through 2c5a21d + current `agent/app-first-use-onboarding` working tree。
- 12:32 +08:00：实现公开 MVP 的“设置与信任中心”首切片：新增 Journey/Review/Settings 主导航，帮助/隐私/条款离线状态页，本地学习数据清理二次确认，以及平台版本信息；外部动作只接受显式配置的有效 `https` URL 或支持邮箱，不提供假链接。清理事务删除学习者进度、掌握度、练习、口语和 Outbox，保留已安装课程包。验证：Flutter format 77 files / 0 changed、analyze 0 issues、77 tests、`git diff --check`；Debug APK 为 177,107,582 bytes，SHA-256 `0aa92dcbe487ef3960dc1fbbec5bba29c947ef088952880f44955bf888b286e9`；隔离 Android 模拟器 Settings 页面和 UI hierarchy 通过。本轮未连接 Sony 真机，真实支持/法律资源仍待产品负责人提供。repo-docs Sync Gate 为 `foreground patch`。Synced through 39e442a + current `agent/app-settings-trust-center` working tree。
- 11:32 +08:00：按用户纠正重新划分“内容里程碑、公开 MVP、商店发布”。源码审计确认生产路由只有 Journey/课程/复习，Go API 只有健康/就绪/元数据端点，客服、隐私/条款、数据控制、账号、同步、删除、订阅和完整运营功能尚未实现。新增 `docs/requirements/public-mvp-completion.md` 作为 G1—G6 单一门禁，明确当前是公开 MVP 产品开发，正式签名、素材和审核提交必须最后开始；同步 README、开发方案、项目现状、实时交接、详细 Agent 基线、经验与 repo guide。Synced through 92f748b + current documentation patch。
- 11:18 +08:00：关闭默认 Release 真机复验门禁。先核对本地与 GitHub Pre-release `v0.2.0` 的 `app-release.apk` 均为 64,512,944 bytes，SHA-256 均为 `98e5a187567147899205a29986ff0b533e188820dffb81ec1636f7d61fb3e601`；APK manifest 为 `com.hieronymusf.mandarinmission` / `0.2.0+3`，Android Debug 证书。Sony `XQ-DQ72` 从旧 `0.1.1+2` 经 `adb install -r` 覆盖升级，`firstInstallTime` 未改变；冷启动后真实 Journey UI hierarchy 精确匹配 Café、Market、Metro 各 4 节课和 1 个地点终局，共 12 节课与 3 个终局，前台 PID 的 fatal、Flutter error 与 `RenderFlex overflowed` 匹配数为 0。repo-docs Sync Gate 为 `foreground patch`；同步 README、课程内容模块与源码证据。Synced through 92f748b + current documentation patch。
- 00:00 +08:00：完整 M2 通过 PR [#20](https://github.com/HieronymusF/mandarin-mission/pull/20) 的 `mobile`、`learning-core`、`api` 三项 CI，并以 merge commit `07154a1` 合并到 `main`；合并后的 `main` CI run [`30209138974`](https://github.com/HieronymusF/mandarin-mission/actions/runs/30209138974) 再次全绿。App 版本为 `0.2.0+3`，内容包保持 `0.2.7` / `release`，GitHub 发布 Pre-release [`v0.2.0`](https://github.com/HieronymusF/mandarin-mission/releases/tag/v0.2.0)，tag 指向 `07154a1`。Release 资产 `app-release.apk` 为 64,512,944 bytes，远端状态 `uploaded`，SHA-256 `98e5a187567147899205a29986ff0b533e188820dffb81ec1636f7d61fb3e601` 与本地一致。内容包是正式 Release；APK 仍由 Android Debug 证书签名，因此只作为安装验收包并标记 Pre-release，不是商店签名包。本轮无连接真机，默认 Release APK 的新一轮安装复验仍单列为后续边界。repo-docs Sync Gate 为 `foreground patch`。Synced through 07154a1 + current documentation patch。

## 2026-07-26

- 23:08 +08:00：用户批准完整 M2 成为默认正式 Release。将内容包改名为 `m2-course.json` 并把状态提升为 `release`，默认 Repository 直接加载 M2，移除 `MM_USE_M2_DRAFT` 入口；App 版本升至 `0.2.0+3`，同步 README、walkthrough、代码地图、课程内容模块、术语表和源码证据。验证通过：内容校验 2 包、learning core 23 tests、Flutter 68 tests、Release APK 64,512,944 bytes / SHA-256 `98e5a187567147899205a29986ff0b533e188820dffb81ec1636f7d61fb3e601`。APK 仍为 debug key 签名，本轮无连接真机；GitHub 发布正在执行，最终 PR、`main` SHA、tag、远端资产与 CI 待发布后写回。repo-docs Sync Gate 为 `foreground patch`。Synced through 91f9b75 + current `feat/m2-course-draft` working tree。
- 22:50 +08:00：用户追问“M2 是什么”，说明术语表只写版本状态、没有先给中文概念。将 M2 明确定义为项目“阶段 2：可玩循环”的第二阶段内容包，并在同一行说明其当前范围为 Café、Market、Metro 3 个地点、12 节课和 3 个地点终局；没有改变代码、内容包状态或发布决策。repo-docs Sync Gate 为 `foreground patch`。Synced through 91f9b75 + current `feat/m2-course-draft` working tree。
- 22:40 +08:00：连续完成 Sony `XQ-DQ72` 上剩余 6 个 M2 单元的人工逐页 UX。Market final challenge 走通 6 个 learner 回合、连续 system 节点、每轮 phrase ticket 门禁重置、总结与 Metro 01 解锁；Metro 01—04 共 43 个步骤的教学卡、27 个教学/听辨/跟读入口、听辨、排序、四次无录音自评、2/2/3/3 个 learner 对话、总结与逐课线性解锁全部通过；Metro final challenge 走通 8 个 learner 回合并进入总结。最终 Journey 的 12 课与 3 个地点终局全部显示 `Completed`，清空后的 PID logcat 中 rendering/fatal 模式计数为 0。新增范围 14/14 单元人工逐页 UX 至此关闭；默认 Provider 与 `release` 仍待产品批准。本段未录音、未改代码、未重跑自动化门禁；最终截图为 `journey-all-m2-complete.png`。repo-docs Sync Gate 为 `foreground patch`。Synced through 91f9b75 + current `feat/m2-course-draft` working tree。
- 21:55 +08:00：完成 Sony `XQ-DQ72` 上 Market 04 Step 1—10 人工逐页 UX。四张教学卡无溢出，4 条教学音频、听辨和跟读共触发 6 次 Android AudioTrack；听辨“要袋子吗？”，按“不 → 要 → 袋子”完成排序，经无录音自评后，两轮 phrase ticket 分别核对“不要袋子。”与“谢谢。”，动作门禁在“一共十二块。”后重新锁定，学习者终止节点直接进入总结。返回 Journey 后 Market 04 显示 `Completed`，Market final challenge 为 `Ready to start`，Metro 01 保持 `Locked`；PID `26678` 的 logcat 无 Flutter rendering 或 fatal exception，设备已进入 Market final challenge Step 1。证据为 `market04-step01.png`—`market04-step10.png`、`journey-after-market04-market.png` 与 `market-final-step01-ready.png`。本段未录音、未改代码，未把此前自动化结果冒充本轮重跑。repo-docs Sync Gate 为 `foreground patch`。Synced through 91f9b75 + current `feat/m2-course-draft` working tree。
- 21:41 +08:00：完成 Sony `XQ-DQ72` 上 Market 03 Step 1—11 人工逐页 UX。五张教学卡与“我要那个大的。”长句无溢出，5 条教学音频、听辨和跟读共触发 7 次 Android AudioTrack；听辨“这个可以吗？”，按“我 → 要 → 那个 → 大的”完成排序，经无录音自评后，两轮 phrase ticket 分别核对“不要这个。”与“我要那个大的。”，动作门禁在第二轮重新锁定，对话以系统“好的。”结束。返回 Journey 后 Market 03 显示 `Completed`，Market 04 为 `Ready to start`，Market final challenge 保持 `Locked`；PID `26678` 的 logcat 无 Flutter rendering 或 fatal exception，设备已进入 Market 04 Step 1。证据为 `market03-step01.png`—`market03-step11.png`、`journey-after-market03-market.png` 与 `market04-step01-ready.png`。本段未录音、未改代码，未把此前自动化结果冒充本轮重跑。repo-docs Sync Gate 为 `foreground patch`。Synced through 91f9b75 + current `feat/m2-course-draft` working tree。
- 21:31 +08:00：完成 Sony `XQ-DQ72` 上 Market 02 Step 1—10 人工逐页 UX。四张教学卡与“我要两个苹果。”长句无溢出，四个示例音频触发 Android 播放链；听辨“要几个？”，按“我 → 要 → 两个 → 苹果”完成排序，经无录音自评和 phrase ticket 发送“我要两个苹果。”后，对话以系统“好的。”结束。返回 Journey 后 Market 02 显示 `Completed`，Market 03 为 `Ready to start`，Market 04 与地点终局保持 `Locked`；PID `26678` 的 logcat 无 Flutter rendering 或 fatal exception，设备已进入 Market 03 Step 1。证据为 `market02-step01.png`—`market02-step10.png`、`journey-after-market02-market2.png` 与 `market03-step01-ready.png`。本段未录音、未改代码，未把此前自动化结果冒充本轮重跑。repo-docs Sync Gate 为 `foreground patch`。Synced through 91f9b75 + current `feat/m2-course-draft` working tree。
- 21:17 +08:00：完成 Sony `XQ-DQ72` 上 Market 01 Step 1—11 人工逐页 UX。五张教学卡与长句无溢出；听辨与语序正确路径、无录音降级自评、两个 learner 回合的 phrase ticket 和动作门禁均通过，对话按“您好。→ 这个多少钱？→ 三块。→ 谢谢。”结束。返回 Journey 后 Market 01 显示 `Completed`，Market 02 为 `Ready to start`，Market 03 仍为 `Locked`；当前进程自 21:10 起的 logcat 无 Flutter rendering 或 fatal exception，设备已进入 Market 02 Step 1。证据为 `market01-step1-start.png`、`market01-step02.png`—`market01-step11.png`、`journey-after-market01-market-cards2.png` 与 `market02-step1-ready.png`。本段未录音、未改代码，未把此前自动化结果冒充本轮重跑。repo-docs Sync Gate 为 `foreground patch`。Synced through 91f9b75 + current `feat/m2-course-draft` working tree。
- 20:42 +08:00：完成 Sony `XQ-DQ72` 上 Café final challenge 人工逐页 UX。5 个 learner 回合均通过 phrase ticket 解锁发送，并在推进后重新执行动作门禁；Step 2 总结页无溢出。返回 Journey 后 Café 终局显示 `Completed`，Market 01 正确变为 `Ready to start`；设备已进入 Market 01 Step 1。证据为 `56-cafe-final-ticket1.png`—`65-cafe-final-complete-dialogue.png`、`66-journey-after-cafe-final.png` 与 `69-market01-step1.png`。本段未录音、未改代码，未把此前自动化结果冒充本轮重跑。repo-docs Sync Gate 为 `foreground patch`。Synced through 91f9b75 + current `feat/m2-course-draft` working tree。
- 20:35 +08:00：完成 Sony `XQ-DQ72` 上 Café 04 Step 1—10 人工逐页 UX。Step 1 失败证据显示共享 `scene_intro` 仍写死 Café 01 的地点、问候与“完整订单”提示；最小修复改为只呈现 location Badge 与 authored `step.text`。首轮英文/中文复核共同拦截不自然且与 Metro 01 场景矛盾的 `At the metro`，删除地点推导句并补 Market/Metro Widget 回归后，两名原 Agent 最终均为 15/15 通过，Draft 升至 `0.2.7`。验证：learning core format 7 files / analyze 0 issues / 23 tests；Flutter format 67 files / analyze 0 issues / 67 tests；M2 Debug APK 构建和 `install -r` 通过，修复前后真机截图为 `36-cafe04-step1.png` 与 `54-cafe04-step1-final.png`。设备停在 Café final challenge Step 1。repo-docs Sync Gate 为 `foreground patch`。Synced through 91f9b75 + current `feat/m2-course-draft` working tree。
- 20:05 +08:00：继续 M2 人工逐页 UX，完成 Sony `XQ-DQ72` 上 Café 02 Step 4—10 与 Café 03 Step 1—10。失败证据显示共享教学卡错误推断课程前两个知识点为每页 `Build the order`，听辨/排序/总结反馈写死 Café 01 语义，无音频对话仍写 `Listen`，phrase ticket 与终止态残留原支持文案。最小修复移除推断卡、按知识点显示 `WORD/PHRASE/SENTENCE`、改为题型通用反馈、收敛对话状态支持并让总结页读取本课 outcome；learner-facing 英文变化把 Draft 升至 `0.2.6`。英文编辑与中文教学 Agent 对 12 课和 3 个合成终局最终均为 15/15 通过。验证：内容校验 2 包；learning core format 9 files / analyze 0 issues / 23 tests；Flutter format 67 files / analyze 0 issues / 65 tests；M2 Debug APK 构建、`install -r` 与 Café 03 最终总结页真机截图通过。下一页从 Café 04 继续。Synced through 91f9b75 + current `feat/m2-course-draft` working tree。
- 19:04 +08:00：新增 `integration_test/m2_process_persistence_app.dart`，用只安装一次的验收 APK 关闭 M2 真实磁盘/进程冷启动门禁。Sony 首次 PID `21038` 向独立 106,496-byte Drift 文件写入 15 条课程/终局完成记录、208 条掌握度和一次复习；ADB `force-stop` 后 PID 为空，第二次 PID `21209` 从同一文件恢复全部状态，真实 Journey UI hierarchy 列出 12 课与 3 个终局均为 `Completed`。隔离数据库已按明确路径删除，默认 Café Debug 已恢复。首次尝试拆成两次 `flutter test integration_test` 时，测试框架卸载 App 并清除沙箱；该失败已记录为设备验收规则。移动端门禁为 format 67 files / 0 changed、analyze 0 issues、62 tests。repo-docs Sync Gate 为 `foreground patch`；人工逐页 UX 与发布决策仍待完成。Synced through 91f9b75 + current `feat/m2-course-draft` working tree。
- 18:44 +08:00：新增 `integration_test/m2_device_acceptance_test.dart` 并在 Sony `XQ-DQ72`（Android 15）通过两项真机测试：49/49 条新增 M2 音频逐条从 `playing` 到 `stopped`；真实 M2 包的 12 课、3 个终局、208 条掌握度状态、一次到期复习和 Provider 重载全部通过。测试使用隔离内存数据库且播放器音量为 0，不污染手机正式数据，也不冒充人工听感或逐页 UX。M2 Debug 初始 Journey 锁定界面已截图；随后用 `install -r` 恢复默认 Café Debug，并由 UI hierarchy 确认只剩 `Order one coffee`。移动端门禁为 format 67 files / 0 changed、analyze 0 issues、62 tests。repo-docs Sync Gate 为 `foreground patch`。人工逐页 UX、真实进程冷启动和正式发布评审仍待完成。Synced through 91f9b75 + current `feat/m2-course-draft` working tree。
- 17:46 +08:00：完成 M2 地点终局与单线解锁最小切片。运行时将未内嵌的 location `challengeId` 合成为两步课程，Journey 按持久化先修状态逐课开放、四课后开放终局、终局完成后开放下一地点；M1 内嵌 `cafe-challenge` 不生成重复卡。默认构建继续加载 Café Release，`MM_USE_M2_DRAFT=true` 只用于 M2 验收构建。失败测试先确认 `cafe-02` 不应初始可用，修正后目标 12 tests、Flutter analyze 0 issues、全量 62 tests、内容校验 2 包、默认与 M2 两个 Debug APK、`git diff --check` 通过；M2 APK 在模拟器显示完整初始锁定状态，进入真实 Café 01 并成功播放一条 M2 音频。repo-docs Sync Gate 为 `foreground patch`，validator 为 0 errors / 1 个既有 broad-value warning。三个地点完整设备实走、49 条音频逐条运行时播放和完整复习闭环仍待完成。Synced through 91f9b75 + current `feat/m2-course-draft` working tree。
- 17:08 +08:00：M2 Draft 加入 `content` package，并通过稳定资产常量供 `CourseContentRepository` 显式选择；默认 Provider 继续加载 Café Release。测试先因缺少 M2 Repository 常量编译失败，接线后核对 3 locations、12 lessons、15 dialogues、53 ready assets，并从 asset bundle 加载 49 条新增 WAV。验证：内容校验 2 包，learning core format 9 files / 0 changed、analyze 0 issues、23 tests，Flutter format 66 files / 0 changed、analyze 0 issues、61 tests、Debug APK；APK 同时包含 Café Release 与 M2 Draft。repo-docs Sync Gate 为 `foreground patch`；地点终局入口、线性解锁、真实音频播放和持久化仍待完成。Synced through 91f9b75 + current `feat/m2-course-draft` working tree。
- 16:52 +08:00：`dialogue_turn` 从固定首轮升级为按 `nextNodeId` 推进的多轮播放器。每个 learner 轮次重新要求 `I said it aloud` 或 phrase ticket；系统终止节点先显示收尾，学习者终止节点完成动作后结束步骤；离页后 `autoDispose` 清理当前轮次。真实 M2 测试完整遍历 Café、Market、Metro 三个地点终局的 5/6/8 个 learner 轮次，并发现、覆盖 Market 连续系统节点。验证：目标测试与 M1 App 流程 4 tests、Flutter format 66 files / 0 changed、analyze 0 issues、61 tests、Debug APK。M2 Fixture 与 language lock 文本未改，package/Repository、地点终局入口、真实音频播放和持久化仍待完成。repo-docs Sync Gate 为 `foreground patch`。Synced through 91f9b75 + current `feat/m2-course-draft` working tree。
- 16:35 +08:00：把 M2 49 条已通过试听的候选收敛为唯一选择，其中 9 条采用后续试听轮默认替换；文件复制到 `apps/mobile/assets/audio/cosyvoice/`，Draft 升至 `0.2.5`，49 条新增资产写回 path/SHA-256/Apache-2.0 credit 并从 `planned` 提升为 `ready`，整个包仍保持 `draft`。验证：49/49 asset ID 和文件名唯一，52 条包内音频哈希与 Fixture 一致；learning core format 0 changed、analyze 0 issues、23 tests，内容校验 2 个包，显式 UTF-8 后 JSON Schema 通过；Flutter format 65 files / 0 changed、analyze 0 issues、59 tests、Debug APK，APK 内 49 条新增 WAV 哈希全部匹配。Schema 首次命令因 Windows GBK 解码 UTF-8 中文失败，设置 `PYTHONUTF8=1` 后通过；这是工具环境失败，不是内容失败。repo-docs Sync Gate 为 `foreground patch`；地点终局多轮播放器和 M2 默认入口仍未完成。Synced through 91f9b75 + current `feat/m2-course-draft` working tree。
- 16:15 +08:00：用户确认 Metro 最后一批“请再说一遍。／请慢一点。／我听不懂。／明白了。”全部可接受，至此 M2 的 Café 12、Market 18、Metro 19 共 49 条仓库外音频候选均已完成人工试听并取得可接受版本。repo-docs Sync Gate 决定为 `foreground patch`：内容模块与源码证据改为“听感候选齐备、接入仍未完成”，同时保留 49 条资产在 Draft 中仍为 `planned`、尚未整理唯一最终选择表、复制到 App、写入 path/SHA-256/credit 或完成播放与打包验证的边界。repo-docs validator 为 0 errors / 1 个既有 broad-value warning。Synced through 91f9b75 + current `feat/m2-course-draft` working tree。
- 14:29 +08:00：M2 Market 人工试听连续发现两类对话韵律问题：“不要这个。”句末像仍有后文，而“要袋子。”作为“要袋子吗？”的肯定回答时信息焦点应落在“要”。这两次纠正已去重并入 `AGENT_LESSONS.md` 与 `docs/content-authoring.md` 的 TTS 门禁，要求对话音频与前后轮次连续试听并审核重音、信息焦点和句末完整性。repo-docs Sync Gate 决定为 `foreground patch`：内容模块与源码证据不再误写 49 条 M2 音频“尚未生成”，改为仓库外技术候选已生成并逐条试听、仍未接入 Draft/App 或提升为 `ready`；精确试听进度继续由根 `HANDOFF.md` 持有。repo-docs validator 为 0 errors / 1 个既有 broad-value warning。Synced through 91f9b75 + current `feat/m2-course-draft` working tree。
- 12:42 +08:00：按用户要求由主 Agent 编排英文编辑 Agent `/root/m2_english_editor` 与中文教学 Agent `/root/m2_chinese_teacher`，独立审核 Café 02—04、Market 01—04、Metro 01—04 和 3 个地点终局共 14 个单元；首次分别为 8/14、9/14 通过。主 Agent 修正英文自然度和语义对齐，把 Metro 01 改为“请问，地铁站在哪里？”，Metro 03 改为“去火车站坐几号线？”，重排 Market 终局选品/数量顺序并补齐 Metro 终局的询路、购票、问线路和换乘角色；Draft 升至 `0.2.4`。两名原 Agent 复核后均为 14/14 通过，课程文本 language lock；两者明确以 AI 身份审核，不冒充真人或母语者，用户无需参与逐条审校。首次内容校验正确拦截 Metro 01 词块标点不一致，修正后 learning core format 0 changed、analyze 0 issues、23 tests，内容校验 2 个包，M2 JSON Schema 和 repo-docs validator（0 errors / 1 个既有 warning）通过。独立 Schema 命令前两次因系统与工作区 Python 都缺 `jsonschema` 而未启动，改用 `uv run --with jsonschema` 后通过。49 条 planned 音频、地点终局多轮播放和 App 接入仍未完成。Synced through 91f9b75 + current `feat/m2-course-draft` working tree。
- 11:56 +08:00：完成 M2 Metro 01—04 AI 预审，Draft 升至 `0.2.3`；四节完成页改为学习者结果，英文释义按乘车语境收敛，Metro 01、03 与地点终局增加系统问候入口，Metro 04 首个学习者回复改用“请再说一遍”，请求放慢后分段重复同一条换乘指令。范围测试先按预期失败于 `metro-01` 起点为 `learner`，修订后 Café、Market、Metro 共 11 节新增课程均满足当前 `DialogueStep` 的 `system → learner(itemId)` 首轮形状。两次 core 门禁均在 format dry-run 阶段发现同一测试文件未格式化，显式运行 formatter 后重跑为 format 0 changed、analyze 0 issues、23 tests；内容校验通过 2 个包，M2 JSON Schema 通过，repo-docs validator 为 0 errors / 1 个既有 broad-value warning，`git diff --check` 通过。英语母语编辑、国际中文教师、49 条 planned 音频、地点终局多轮可玩性和 App 接入仍未完成。Synced through 91f9b75 + current `feat/m2-course-draft` working tree。
- 11:42 +08:00：完成 M2 Market 01—04 AI 预审，Draft 升至 `0.2.2`；四节完成页改为学习者结果，两条购买回复的英文改为 `I'll have ...`，Market 01 增加卖方开场以满足当前 `DialogueStep` 的 `system → learner` 入口，Market 01、Market 04 与地点终局均在学习者致谢处结束。范围测试先按预期失败于 `market-01` 起点为 `learner`，修订后 Café 与 Market 七节已预审课程均通过。验证：第一次 core 门禁发现测试文件尚未格式化，执行 formatter 后重跑为 format 0 changed、analyze 0 issues、23 tests；内容校验通过 2 个包，M2 JSON Schema 通过，repo-docs validator 为 0 errors / 1 个既有 broad-value warning，`git diff --check` 通过。英语母语编辑、国际中文教师和普通话音频签核仍未完成。Synced through 91f9b75 + current `feat/m2-course-draft` working tree。
- 11:31 +08:00：完成 M2 Café 02—04 AI 预审，Draft 升至 `0.2.1`；修正两条英文订单译法、三节学习者完成页文案、Café 04 单轮对话入口和“好的”回应“谢谢”的不自然结尾，并让 Café 地点终局从已出现的问候语开始、在学习者致谢处结束。新增测试锁定当前 `DialogueStep` 只读取 `system → learner` 首轮的契约；该测试先失败于 `cafe-04` 起点为 `learner`，修订后通过。验证：JSON Schema 通过，`verify.ps1 -Scope core` 为 format 0 changed、analyze 0 issues、23 tests，`verify.ps1 -Scope content` 验证 2 个包，repo-docs validator 为 0 errors / 1 个既有 broad-value warning，`git diff --check` 通过。AI 预审不代表英语母语编辑、国际中文教师或普通话音频签核。Synced through 91f9b75 + current `feat/m2-course-draft` working tree。
- 11:14 +08:00：用户确认 Café → Market → Metro、单线解锁和 Metro“火车站”默认方案；新增独立 `m2-course-draft.json`，包含 3 个地点、12 节课、52 个知识点、15 段对话和 53 个资产，其中 49 个新增音频为 `planned`。原 `cafe-course.json` 和 App asset 入口保持不变，M2 Draft 只进入 Schema、内容校验和范围测试，未冒充可玩或可发布内容。验证：JSON Schema 通过，内容 CLI 验证 2 个包，learning core format/analyze/22 tests 通过，repo-docs validator 为 0 errors / 1 个既有 broad-value warning。Synced through 91f9b75 + current `feat/m2-course-draft` working tree。

## 2026-07-25

- 23:15 +08:00：M2 数据驱动 Journey 入口通过 PR [#18](https://github.com/HieronymusF/mandarin-mission/pull/18) 的 `mobile`、`learning-core`、`api` 三项 CI，并以 merge commit `f02ccdc` 合并到 `main`；合并后的 `main` CI run `30162984905` 再次全绿。App 版本升至 `0.1.1+2`，发布 Pre-release [`v0.1.1`](https://github.com/HieronymusF/mandarin-mission/releases/tag/v0.1.1)，标签指向 `f02ccdc`。Release 资产 `app-release.apk` 为 62,721,119 bytes，GitHub API 状态为 `uploaded`，远端 SHA-256 `3f5a1b8a5fd9a021fbdba0311e4ef82358bfc0b9eb91dfbe85832352b1a6c4dc` 与本地一致；APK 仍由 Android Debug 证书签名，只是安装验收包，不是商店上架包。Synced through f02ccdc + current documentation patch。
- 22:51 +08:00：启动 M2 第一个最小切片。Flutter 内容模型开始保留并按 `order` 排序 location；Journey 改为遍历 location `lessonIds` 生成课程卡，卡片标题、步骤数、进度查询和路由 ID 均来自内容包，不再写死 `cafe-01`。新增两地点/两课程 Widget 测试，确认输入顺序为 Market→Café 时仍显示 Café→Market；原咖啡 11 步流程、宽屏对齐、format、analyze、Flutter 58 tests 和 Debug APK 通过。模拟器实际 Fixture 仍只显示已发布的 Café 卡，布局与入口无回归；M2 的 3 地点、12 节正式内容尚未制作。Synced through c75de22。

## 2026-07-24

- 23:53 +08:00：补齐 GitHub 可下载交付：发布 Pre-release [`v0.1.0+1`](https://github.com/HieronymusF/mandarin-mission/releases/tag/v0.1.0%2B1)，标签指向 `main` 提交 `ce25b64`。Release 资产 `app-release.apk` 为 62,720,887 bytes，GitHub API 返回状态 `uploaded`，远端 SHA-256 `235d7e2875d3d5c35040aab3f6078fa960ed8aeae36eed96fa017942e2819792` 与本地一致。该 APK 仍由 Android Debug 证书签名，因此标记为 Android 验收包而非商店正式包。Synced through ce25b64 + current documentation patch。
- 23:21 +08:00：M1 功能提交 `6f89447` 已通过 PR #16 的 `mobile`、`learning-core`、`api` 三项远端 CI，并以 merge commit `d2c83b4` 合并到 `main`；本地 `main` 已快进到同一提交。从合并后 `main` 构建 release 模式 APK，产物为 `apps/mobile/build/app/outputs/flutter-apk/app-release.apk`，62,720,887 bytes，SHA-256 `235d7e2875d3d5c35040aab3f6078fa960ed8aeae36eed96fa017942e2819792`。包内 Fixture 为 `release` / `0.1.7`，三条 CosyVoice 哈希匹配且无旧音频；APK v2 签名验证通过，但证书是 `Android Debug`，因此只能作 release 模式验收包，不是商店上架包。Synced through d2c83b4 + current docs-only working tree。
- 22:43 +08:00：用户通过 CosyVoice I1“我要”、L2“咖啡”与 I3“我要一杯咖啡”最终整组试听。三条音频已替换包内 MeloTTS 资产，路径改为 `assets/audio/cosyvoice/`，Fixture 升至 `0.1.7` 并记录 Apache-2.0 来源、内置 `中文女`、seed 1986、速度、合成文本差异和 PCM16 兼容转换后 SHA-256。Fixture 升为 `release` 后，首次全量回归正确拦截了两个把资产改为 `planned` 却未把测试副本改为 `draft` 的降级测试；最小修正后，内容校验、Flutter format/analyze、57 tests、debug APK、repo-docs validator 和 `git diff --check` 通过。最终 APK SHA-256 为 `d19498171b60b501ba74ed16905a4ed7bb3311241bd40094b3814c5c46cea374`，内含 `release` / `0.1.7` Fixture、三条哈希匹配的 CosyVoice 资产，无 MeloTTS/Kokoro 遗留资源；已覆盖安装到 Sony `XQ-DQ72` 并保留 App 数据。三条音频均曾从真实课程入口播放并正常释放音频焦点；图片与音频的正式内容门禁已关闭。Synced through 236626b + current working tree。
- 21:01 +08:00：按用户选择将三条课程音频从 Kokoro 开发占位切换为免费本地 MeloTTS 0.1.2，使用官方 `myshell-ai/MeloTTS-Chinese`、内置 `ZH` speaker 和 0.94 语速；Fixture 升至 `0.1.6`，稳定 `audioAssetId` 不变，路径改为 `assets/audio/melotts/`，同步 MIT 许可、生成参数和 SHA-256。验证：三条哈希匹配、内容校验、Flutter format（0 changed）、analyze（0 issues）、57 tests、debug APK、APK 内三条 `melotts` 资源且无 `kokoro` 资源。人工试听连续否决三轮：首版短音发音不完整，第二轮音色不一致，第三轮确定性母带长句出现电音；MeloTTS 因此淘汰。仓库外改用 Apache-2.0 `CosyVoice-300M-SFT` 内置 `中文女` 后，J 组把 `fēi` 压成近似 `fi`；同音“咖飞”K 组修正 `/ei/` 后，`kā` 又被指出偏轻声。“咔飞”L1 不通过，“喀飞”L2 同时保留一声和 `/ei/` 并获用户认可，现进入 I1/L2/I3 最终整组复听。全部没有真人克隆或音频后处理，确认前不会覆盖包内文件。Synced through 236626b + current working tree。
- 补齐 `image_choice`、`tone_contrast`、`order_tokens` 三种数据驱动课程步骤，咖啡 Fixture 从 8 步扩展为 11 步、版本升至 `0.1.5`；新增内容契约、错误重试与本地 attempt 保存。声调题比较同一句带调拼音，词块必须重建目标汉字。失败测试先证明 Validator 无约束、Flutter 模型缺 `tokens`；200% 小屏测试又发现并修复音频不可用卡片 284px、最长英文选项 558px 横向溢出。模拟器发现 Journey 硬编码 `8 short steps`，现改为读取课程内容。验证：内容 validator/repository tests、移动端 analyze、57 个 Flutter tests、Debug APK、Go vet/test、repo-docs validator；Android 模拟器与 Sony 真机通过新增三步，真机继续到第 11 步完成页，返回 Journey 显示 `Completed · 11 short steps`，杀进程冷启动后仍保留；随后完成 6 项到期复习并显示 `Review done · Review is up to date`，再次冷启动仍保留。咖啡场景图随后作为 `ready` 资产接入，选项改为中文+拼音；第一版因杯子过大被否决，第二版缩小杯子、放大人物后获用户真机确认，最终 SHA-256 为 `a5fd40ea29477c59dd4aefae658044c251ae3867b50a574503f42ef921d5f011`。Synced through 236626b + current working tree。

## 2026-07-23

- 17:04 +08:00：完整功能批次提交为 `610af84`，推送到 `feat/audio-playback-recording`，PR #14 在 `mobile`、`learning-core`、`api` 三项远端 CI 全部成功后以 merge commit `161c8e1` 合并到 `main`。本地 `main` 已快进到同一提交；功能分支保留。Synced through 161c8e1。
- 16:37 +08:00：修复 Journey 咖啡课程卡硬编码 `Not started` 的问题，改为读取持久化课程进度，完成后显示 100% 与 `Practice again`。Sony `XQ-DQ72` 已完成课程、覆盖安装/两次冷启动进度保留、8 项必做到期复习保存，以及飞行模式完整重走课程；结束后飞行模式已关闭。目标测试先失败再通过；移动端 format、analyze（0 issues）、全量 52 tests 和 debug APK 通过。真实来电因设备无 SIM 延期。Synced through 8072279；工作树仍未提交。
- 16:12 +08:00：同步 Sony `XQ-DQ72` 权限与步骤生命周期真机验收：普通拒绝后的说明/重试、永久拒绝后的 `Open Settings`、设置授权返回后的实时权限刷新，以及录音中从第 6 步返回第 5 步的停止和临时文件清理均通过。修复权限状态缓存未在 `resumed` 刷新、课程步骤返回未等待媒体清理两个问题。验证：移动端 format、analyze（0 issues）、全量 52 tests、debug APK、Sony 真机界面/日志/缓存；Synced through 8072279，工作树仍未提交。
- 15:52 +08:00：修正第 7 步预设对话的静态空壳行为。页面初始隐藏答案并禁用发送，用户需选择 `I said it aloud` 或 `Use phrase ticket`；phrase ticket 会显示整句，随后 `Send reply` 才能进入第 8 步。验证：移动端 format、analyze、全量 51 tests、debug APK，以及 Sony `XQ-DQ72` 两条交互路径和进入第 8 步的真机验收。Synced through 8072279；工作树仍未提交。
- 15:39 +08:00：同步 Sony `XQ-DQ72` 真机证据：课程包内音频播放、麦克风授权、44.1 kHz 录音、音量提示、同文件回放、重录删除当前文件和 Home 切后台停止/清理均通过；约 7 秒 M4A 为 115,228 bytes（约 0.9 MB/分钟）。普通权限拒绝、永久拒绝、真实来电恢复与最终课程音色仍未完成。验证：书写目标 5 tests、移动端 analyze、全量 51 tests、debug APK、Sony 真机日志与界面、repo-docs validator 和 `git diff --check`。Synced through 8072279；工作树仍未提交。
- 09:08 +08:00：将 21KB 根 `AGENTS.md` 拆为 8KB/102 行的项目入口、`docs/architecture.md`、五个局部 `AGENTS.md`、项目级 `$verify-mandarin-mission` Skill、Codex `PreToolUse` 文件安全 Hook、统一验证脚本和 `docs/runbooks/`；代码地图同步新的 Agent 工作流与本地护栏。验证：Hook 自测与阻断样例、TOML 解析、Skill 校验、PowerShell Parser、repo-docs validator（0 errors / 1 既有 broad-value warning）和 `git diff --check`。Synced through 8072279；工作树仍未提交。

## 2026-07-22

- 14:18 +08:00：明确当前 Kokoro `zf_xiaoxiao` 只是“能用但未获最终听感认可”的开发占位，开发流程允许未来替换为许可清晰的商用 TTS，并新增商业发布权、声线授权、数据条款、成本、哈希和重新试听门禁；复习 listening 题已从内容元数据解析 `ready` 音频，`planned`/缺失资源继续书面降级并正确记录 `usedHint`。验证：Flutter format、analyze、49 tests、内容校验、debug APK、模拟器真实到期复习入口与 24 kHz 单声道播放、repo-docs validator 和 `git diff --check`。Synced through 8072279；工作树仍未提交。
- 14:06 +08:00：用户试听否决 `zf_xiaoyi`（speaker 48、0.82 语速）的机械听感；三条课程 WAV 已改用 Kokoro `zf_xiaoxiao`（speaker 47、0.95 语速）重新生成，Fixture 升至 `0.1.3` 并更新 SHA-256/credit。验证：三条哈希匹配、内容校验、课程内容 Repository 6 tests、debug APK、APK 资源条目、repo-docs validator 和 `git diff --check`；新候选仍待用户试听确认。Synced through 8072279；工作树仍未提交。

- 13:55 +08:00：用 Apache-2.0 Kokoro-82M v1.0 / `sherpa-onnx 1.13.4` 的 `zf_xiaoyi` 声线替换三条不可发布预览，Fixture 升至 `0.1.2` 并更新 path/SHA-256/来源；新 APK 在模拟器课程第 2 步交付 17,604 帧，与新“我要”WAV 一致。验证：内容校验、Flutter format/analyze、49 tests、debug APK、模拟器播放、repo-docs validator（0 errors / 1 既有 warning）和 `git diff --check`；普通话人工试听仍待完成。Synced through 8072279；工作树仍未提交。
- 13:36 +08:00：接入三条 Windows `Microsoft Huihui Desktop (zh-CN)` WAV 开发预览，记录 path/SHA-256/不可发布限制；Android 模拟器从课程第 2 步完成真实播放。验证：文件哈希、内容校验、Flutter analyze、49 tests、debug APK、模拟器 AudioFlinger 日志和 repo-docs validator（0 errors / 1 既有 warning）。Synced through 8072279；工作树仍未提交。
- 13:20 +08:00：同步录音回放失败状态；保留当前临时录音，显示专用错误说明，并允许原地重试同一路径。验证：移动端 analyze、49 tests、debug APK 和 repo-docs validator（0 errors / 1 既有 warning）通过。Synced through 8072279；同时核对当前未提交工作树。
- 13:10 +08:00：同步媒体错误恢复和普通权限拒绝反馈；播放与录音失败有当前步骤内重试，拒绝麦克风后显示独立状态、再次授权入口和无录音自评说明。验证：移动端 analyze、47 tests 和 debug APK 通过。Synced through 8072279；同时核对当前未提交工作树。
- 11:55 +08:00：同步课程媒体生命周期行为；记录切后台、页面离开、重录和释放时停止媒体并清理当前临时录音，以及对应 Widget/Controller 测试。
- 首次建立中文 repo guide。
- 以“完成点咖啡课程并进入到期复习”为主 walkthrough。
- 增加整个一方源码范围的代码地图，以及内容契约、本地学习闭环、媒体降级三个模块。
- 记录两轮源码取证、读者模拟、反证检查和当前未确认边界。
- 同步媒体组件行为：loading 反馈、不可用降级优先级，以及 4 项组件 Widget 测试。
- 验证：移动端 format、analyze、46 tests 和 debug APK 通过；`validate_repo_docs.py` 为 0 errors。代码地图因每个目录按规则重复“重要代码”表头保留 1 条 broad-value warning。
- 当时基线为 8ebcb77；同时核对 2026-07-22 当前 `feat/audio-playback-recording` 未提交工作树。

当前同步锚点：Synced through f03220e + current documentation patch。

后续只记录会改变读者模型、阅读路径或证据范围的更新；临时调试状态继续放在根目录 `HANDOFF.md`。
