name: mandarin_mission_content
description: "Versioned course content assets for Mandarin Mission."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

flutter:
  assets:
    - fixtures/cafe-course.json
